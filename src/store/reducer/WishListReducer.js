const initialState = {
    basket:[],
}

export default function WishListReducer( state ={...initialState}, action) {

    const product = action?.payload

    
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}